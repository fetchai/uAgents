repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.0
    files: ^python/
    hooks:
      - id: ruff
        exclude: ^(integrations/.*)
        args: [--fix]
      - id: ruff-format
